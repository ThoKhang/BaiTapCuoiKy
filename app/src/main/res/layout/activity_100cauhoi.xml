<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0CC0D9">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingLeft="15dp"
        android:paddingTop="40dp"
        android:paddingRight="15dp"
        android:paddingBottom="15dp">

        <!-- HEADER -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="16dp"
            android:orientation="vertical">

            <!-- Hàng đầu: Back – Title – Question -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical">

                <!-- Nút Back -->
                <ImageButton
                    android:id="@+id/btnBack"
                    android:layout_width="40dp"
                    android:layout_height="40dp"
                    android:background="@android:color/transparent"
                    android:src="@drawable/arrow_back"
                    app:tint="#FFFFFF" />

                <!-- Tiêu đề ở giữa -->
                <TextView
                    android:id="@+id/tvTitleTop"
                    android:layout_width="0dp"
                    android:layout_height="wrap_content"
                    android:layout_weight="1"
                    android:text="Câu hỏi 1/100"
                    android:textSize="22sp"
                    android:textStyle="bold"
                    android:textColor="#FFFFFF"
                    android:gravity="center" />

                <!-- Icon Question -->
                <ImageView
                    android:id="@+id/btnHelp"
                    android:layout_width="32dp"
                    android:layout_height="32dp"
                    android:src="@drawable/question"
                    android:contentDescription="Help"
                    app:tint="#FFFFFF" />
            </LinearLayout>

            <!-- Mô tả -->
            <TextView
                android:id="@+id/tvHeaderDesc"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Học mỗi ngày – từng bước sẽ đưa bạn gần hơn đến mục tiêu!"
                android:textColor="#FFFFFF"
                android:textSize="16sp"
                android:gravity="center"
                android:layout_marginTop="6dp" />

        </LinearLayout>


        <!-- Khung câu hỏi trắng bo góc -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:layout_margin="16dp"
            android:background="@drawable/bg_question_box"
            android:padding="20dp"
            android:elevation="4dp"
            android:clipToOutline="true"
            android:backgroundTint="#FFFFFF"
            android:shape="rectangle">

            <TextView
                android:id="@+id/tvQuestion"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Bạn Lan có 2 cái bút chì, mẹ mua thêm cho Lan 3 cái bút chì nữa. Hỏi Lan có tất cả mấy cái bút chì?"
                android:textSize="20sp"
                android:textColor="#000000" />
        </LinearLayout>

        <!-- Danh sách đáp án -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:paddingStart="32dp"
            android:paddingEnd="32dp"
            android:paddingBottom="32dp">

            <!-- Đáp án 1 -->
            <Button
                android:id="@+id/btnAns1"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="5"
                android:background="@drawable/answer_bg"
                android:textColor="#000000"
                android:textSize="18sp"
                app:backgroundTint="@null"
                android:layout_marginTop="12dp" />

            <!-- Đáp án 2 -->
            <Button
                android:id="@+id/btnAns2"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="1"
                android:background="@drawable/answer_bg"
                android:textColor="#000000"
                android:textSize="18sp"
                app:backgroundTint="@null"
                android:layout_marginTop="12dp" />

            <!-- Đáp án 3 -->
            <Button
                android:id="@+id/btnAns3"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="4"
                android:background="@drawable/answer_bg"
                android:textColor="#000000"
                android:textSize="18sp"
                app:backgroundTint="@null"
                android:layout_marginTop="12dp" />

            <!-- Đáp án 4 -->
            <Button
                android:id="@+id/btnAns4"
                android:layout_width="match_parent"
                android:layout_height="55dp"
                android:text="3"
                android:background="@drawable/answer_bg"
                android:textColor="#000000"
                android:textSize="18sp"
                app:backgroundTint="@null"
                android:layout_marginTop="12dp" />

        </LinearLayout>

    </LinearLayout>

</ScrollView>
