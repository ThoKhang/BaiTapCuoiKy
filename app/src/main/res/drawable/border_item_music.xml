<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Độ bo góc -->
    <corners android:radius="12dp" />

    <!-- Viền màu xám -->
    <stroke
        android:width="1dp"
        android:color="#CCCCCC" />

    <!-- Màu nền (tuỳ chọn) -->
    <solid android:color="@color/blue_settings" />

</shape>